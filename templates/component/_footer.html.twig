{% block footer %}


    {% set links = [
        {'title': 'Mentions Légales', 'path': 'content_show', 'slug': 'mentions-legales'},
        {'title': 'CGU', 'path': 'content_show', 'slug': 'conditions-d-utilisation'},
        {'title': 'crédits', 'path': 'content_show', 'slug': 'credits'},
        {'title': 'contact', 'path': 'content_show', 'slug': 'contact'},
    ] %}

<footer class="bg-light text-center text-lg-start text-white">

    <!-- Copyright -->
        <div class="text-center footer-back p-3"> Copyright &copy; {{ "now"|date('Y') }}
        <a class="text-white text-decoration-none" href="{{ path("home") }}">World Wide Job</a>
            | Développé avec 💙 par les <a href="https://www.wildcodeschool.com/" target="_blank">Wilders</a>
            <div class="d-flex">
                <article class="mx-auto">
            {% for link in links %}
                <a href="{{ path(link.path, {'slug': link.slug}) }}" class="text-decoration-none text-white" > {{ link.title }}</a> |
            {% endfor %}
                </article>
           </div>
    </div>
    <!-- Copyright -->
</footer>

{% endblock %}
