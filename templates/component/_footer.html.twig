{% block footer %}


    {% set links = [
        {'title': 'Mentions Légales', 'path': 'content_show', 'slug': 'mentions-legales'},
        {'title': 'CGU', 'path': 'content_show', 'slug': 'conditions-d-utilisation'},
        {'title': 'crédits', 'path': 'content_show', 'slug': 'credits'}
    ] %}

<footer class="bg-light text-center text-lg-start text-white">

    <!-- Copyright -->
        <div class="text-center footer-back p-3"> Copyright &copy; {{ "now"|date('Y') }}
        <a class="text-dark" href="{{ path("home") }}">World Wide Job</a>
            <div class="d-flex">
                <article class="mx-auto">
            {% for link in links %}
                <a href="{{ path(link.path, {'slug': link.slug}) }}"> {{ link.title }}</a> |
            {% endfor %}
            <p>  contact </p>
                </article>
           </div>
    </div>
    <!-- Copyright -->
</footer>

{% endblock %}
