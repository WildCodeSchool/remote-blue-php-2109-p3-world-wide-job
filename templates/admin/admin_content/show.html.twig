{% extends 'base.html.twig' %}

{% block title %}Content{% endblock %}

{% block body %}
    <h1>Content</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ content.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ content.title }}</td>
            </tr>
            <tr>
                <th>Body</th>
                <td>{{ content.body }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ content.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_content_index') }}">back to list</a>

    <a href="{{ path('admin_content_edit', {'id': content.id}) }}">edit</a>

    {{ include('admin/admin_content/_delete_form.html.twig') }}
{% endblock %}
