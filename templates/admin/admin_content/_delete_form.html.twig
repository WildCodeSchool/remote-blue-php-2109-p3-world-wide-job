<form method="post" action="{{ path('admin_content_delete', {'id': content.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ content.id) }}">
    <button class="btn">Delete</button>
</form>
